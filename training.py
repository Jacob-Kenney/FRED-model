from torch.utils.data import DataLoader

def train(model, dataloaders: tuple[DataLoader, DataLoader], loss_function, optimizer, epochs: int = 50, learning_rate: float = 1e-3, save_path: str = "model.pth"):
    # Initialise model
    # Load dataset
    # Train model
    # Save best model
    pass