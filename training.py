def train(model_class, dataset_name: str, Np: int, Nf: int, hidden_dim: int = 128, num_layers: int = 2, dropout: float = 0.1, process_function = None, batch_size: int = 32, epochs: int = 50, learning_rate: float = 1e-3, shuffle: bool = True, save_path: str = "model.pth"):
    # Initialise model
    # Load dataset
    # Train model
    # Save best model
    pass